import {Content, Spinner} from 'native-base';
import React from 'react';

const spinner = () => {
  return (
    <Content>
      <Spinner color="red" />
    </Content>
  );
};

export default spinner;
